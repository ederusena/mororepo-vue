{
  "name": "monorepo-project",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "start": "yarn workspace react-dsx start & yarn workspace vue-dsx start"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
